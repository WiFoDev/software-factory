{
  "name": "software-factory",
  "version": "0.0.0",
  "private": true,
  "description": "Project-agnostic toolkit for AI-driven software factories",
  "license": "MIT",
  "author": "Luis (WiFoDev)",
  "packageManager": "pnpm@10.26.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "check": "biome check .",
    "check:fix": "biome check --write .",
    "release": "pnpm typecheck && pnpm test && pnpm check && pnpm -r build && pnpm publish -r --access public"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
